platform :ios, '10.0'

target 'ELDebate' do
  use_frameworks!

  pod 'Anchorage', '~> 3.0'
  pod 'Sourcery', '~> 0.6'
  pod 'SwiftLint', '~> 0.18'

  target 'ELDebateTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
