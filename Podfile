platform :ios, '10.0'

target 'ELDebate' do
  use_frameworks!

  pod 'Anchorage', '~> 3.0'
  pod 'Sourcery', '~> 0.6'
  pod 'SwiftLint', '~> 0.18'

  target 'ELDebateTests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.1'
    pod 'Nimble', '~> 6.1'
  end

end
